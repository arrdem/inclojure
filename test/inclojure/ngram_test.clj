(ns inclojure.ngram-test
  (:require [clojure.test :refer :all]
            [inclojure.ngram :as ngram]))

; (deftest start
;   (is (= "<S>" (ngram/start {:forward true})))
;   (is (= "</S>" (ngram/start {:forward false}))))

; (deftest start
;   (is (= "</S>" (ngram/start {:forward true})))
;   (is (= "<S>" (ngram/start {:forward false}))))

